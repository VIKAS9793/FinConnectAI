# Environment Variables Template
# Replace all placeholder values with your actual configuration

# Database Configuration
DATABASE_URL=REPLACE_WITH_YOUR_DATABASE_URL

# Model Configuration
MODEL_PATH=REPLACE_WITH_PATH_TO_MODEL

# Application Settings
LOG_LEVEL=INFO
PORT=8000

# Note: This is a template file. All values must be replaced with actual values before deployment.
# Never commit actual credentials or sensitive information to version control.
